# Format Code

Format code with black and ruff auto-fix.

## Commands

```bash
cd week4 && python -m black .
cd week4 && python -m ruff check . --fix
```

## Notes

- black formats Python code consistently
- ruff fixes linting issues automatically
- Run before committing changes
