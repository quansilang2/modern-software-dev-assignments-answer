# Run Linting Checks

Run ruff to check for code quality issues without auto-fixing.

## Command

```bash
cd week4 && python -m ruff check .
```

## Notes

- This checks without auto-fixing
- Use `/test-format` to auto-fix issues
- Common issues: unused imports, undefined names, style violations
